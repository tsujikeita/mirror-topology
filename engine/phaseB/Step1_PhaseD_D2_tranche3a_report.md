# Phase D-2 tranche ③a v2 報告（監査 RD2T3A-A/B/C の反映）
2026-09-22。Claude作成。engine `step1_engine 0.74.0`（数値 kernel は 0.54.0 基盤の継承；変更は `d2_bank.py` の追加関数と共有 validator の purpose 一般化，版番号）。tranche ② v4 は監査で実装受入れ PASS。本 tranche は ③ の前半（sandbox）で，生成 script／notebook・W₂ position bank・小規模計時・外側 receipt は ③b（次）。**見込み：監査 1〜2 往復（保証ではない）。** **テスト 1198/1198 PASS**（9 chunk・JUnit 同梱 `regression_logs/d2_t3av2_pytest/`；node 集合＝collect・重複 0・failure 0）。

| 実装 | 契約 | 試験（`tests/test_d2_tranche3a.py`） |
|---|---|---|
| `generate_fitting_bank` | purpose **fitting**（family の fitting group；formal key の purpose id 300），batch 0，K=N_fit/m=2000，f64 のみ，1 shard；配置（3 role）／reference（ref_matched）；tranche ② と同じ fresh attempt・in-memory 検証・validate-then-publish；共有 validator を purpose ごとに一般化（evaluation：2 batch・shard 表・BATCHES 由来の K／fitting：batch 0・f64・K=2000・1 shard・fitting group・m=100） | manifest の purpose／group／K／key purpose id／selection，fresh 拒否，fitting latent が evaluation latent と異なること |
| `intake_registered_bank`（強い intake） | (config_id, system) の `BankSupply` を完成 cache から組立て：evaluation batch {0,1}・family reference batch {0,1}・fitting・reference fitting の**全 directory を `verify_bank_dir` で再検証**（bytes／arrays／範囲／正規 spec との identity），family／group／表／spec の一致，**root SHA＝呼出し側の D-1 root と等方 root**，reference と配置が同 latent（batch ごとの cluster 数・scale・cid 一致），fitting の pairing（同 K・同 cid），`formal=True` では全 directory が formal，**cov_manifest は当該配置の D-1 sidecar**（file／array SHA を正規 spec で束縛）。返値：T1/T2 model／ref（batch 0→1 の連結），正式 `ClusterUID`，`batches={0:(0,N₀),1:(N₀,N_max)}`（scale 相当），`FittingBank`，cov_manifest（deep copy） | matched／native 両系統の組立て（UID・batches・fitting K・reference 配列の一致），E7/L1.00 の 3 位置＋family reference から `build_family_input`（正式 `BootstrapPlan`／`FittingPlan` を bank UID から構築）→ `ConfigBank` の N₀／N₄ view；拒否：self-test bank を formal で使用，batch 欠落，root 不一致，別配置の directory，pairing しない fitting，別配置の covariance sidecar，改変 shard |

**v2（監査 ③a：正常経路＝30 配置＋4 参照の fitting 生成・再現，両系統の BankSupply，E7 全 9 配置の `build_family_input`，失敗時の非公開は確認；3 領域を参考補強（`d2_bank.py` 1 file）採用で修正）**：**RD2T3A-A** caller の D-1 sidecar を，固定 `D1_REGISTERED_RECEIPT.receipt_file_sha256` で読んだ receipt の `registered.files` から辿った当該 config の真正 sidecar（同 bytes を読み・hash・JSON 化）と**全内容**で照合（`manifest={}`・別配置の真正 manifest への置換・run_config／tag／env_fingerprint の改変を拒否）；4 root は float 変換前に有限・実数 numeric・21×21 を検査（`S+2jI`・441 要素 1 次元・文字列配列を拒否；変換後も finite）；`formal` は strict bool（None／0／[] を拒否）；fitting 生成で任意 family 引数と配置 family の矛盾を scan 前に拒否。**RD2T3A-B** `BankSupply` を返す前に 2 論理 batch が**同 scale の N₀＋3N₀**（共通 m・連続 UID／row）であることを検査（scale .0002+.0004＝7N₀，.0002+.0001＝2.5N₀ の正規 cache 組合せを拒否；既存 `ConfigBank` と契約一致）。**RD2T3A-C** `_load_role` は実際に使う NPZ の bytes を一度読み，その同じ bytes を manifest の SHA へ照合してから BytesIO で復号（検証後に path を無検査で読み直さない；検証後改変の対照を拒否）。監査の独立 43 対照（提出版 22 PASS／21 FAIL → 43/43）を `tests/test_audit_d2_tranche3a_contracts_chatgpt.py` として同梱（path と出力先の適応のみ）。evaluation generator・共有 validator・spec・CRN 表・adapter・kernel は不変。

限定：試験は TEST-ONLY 線形 kernel・scale 生成。正式 bank・環境／source gate・外側 receipt・W₂ position bank・計時は ③b。

先生の作業：本 packet を ChatGPT の監査へ。閉鎖後 ③b。
